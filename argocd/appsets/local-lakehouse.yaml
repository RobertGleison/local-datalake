apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: local-lakehouse
  namespace: argocd
spec:
  goTemplate: true
  generators:
    - matrix:
        generators:
          - git:
              repoURL: https://github.com/RobertGleison/local-lakehouse
              revision: main
              files:
                - path: argocd/clusters/*.json
          - git:
              repoURL: https://github.com/RobertGleison/local-lakehouse
              revision: main
              files:
                - path: argocd/appsets/services.json
  template:
    metadata:
      name: '{{ .service }}'
      namespace: argocd
      annotations:
        argocd.argoproj.io/sync-wave: '{{ .wave }}'
    spec:
      project: infrastructure
      source:
        repoURL: https://github.com/RobertGleison/local-lakehouse
        path: '{{ .path }}'
        targetRevision: main
      destination:
        server: '{{ .server }}'
        namespace: local-lakehouse
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
